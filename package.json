{
  "name": "movie-recommendation-engine",
  "version": "1.0.0",
  "description": "A graph database application for movie recommendations using CognoDB",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "seed": "node server/seed.js",
    "build": "cd client && npm run build",
    "client": "cd client && npm start"
  },
  "keywords": ["graph-database", "cognodb", "neo4j", "recommendation-engine"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "neo4j-driver": "^5.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
